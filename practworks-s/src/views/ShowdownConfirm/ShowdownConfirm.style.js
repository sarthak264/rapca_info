import styled from "styled-components";

const ModalWrapper = styled.div`
  & > p {
    text-align: center;
    font-size: 20px;
  }
  .btn-wrapper {
    margin-top: 20px;
    display: flex;
    column-gap: 1rem;
  }
`;

export default ModalWrapper;
